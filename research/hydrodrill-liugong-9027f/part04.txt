g);
      background: var(--accent-deep);
      color: #f5f7ee;
    }
    .cost-total small { font: 800 11px/1.3 Arial, sans-serif; letter-spacing: .12em; text-transform: uppercase; color: #d2d8b8; }
    .cost-total strong { display: block; margin-top: 10px; font-size: clamp(38px, 5vw, 60px); line-height: .98; }
    .cost-total p { margin: 13px 0 0; color: #dfe5d8; font-size: 14px; }
    .cost-breakdown {
      padding: 22px;
      border: 1px solid var(--line);
      border-radius: var(--radius-lg);
      background: var(--paper);
    }
    .cost-row { margin-top: 15px; }
    .cost-row:first-child { margin-top: 0; }
    .cost-row-head { display: flex; justify-content: space-between; gap: 12px; font: 800 12px/1.3 Arial, sans-serif; }
    .cost-row-head span:last-child { color: var(--muted); }
    .cost-track { height: 11px; margin-top: 7px; border-radius: 999px; background: var(--line); overflow: hidden; }
    .cost-track span { display: block; height: 100%; border-radius: inherit; background: linear-gradient(90deg, var(--ochre), var(--accent)); }

    .note {
      margin-top: 18px;
      padding: 15px 17px;
      border: 1px solid var(--line);
      border-radius: 12px;
      background: var(--info-soft);
      color: var(--ink);
      font-size: 14px;
    }

    .deep-intro {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 18px;
      align-items: end;
      margin-bottom: 18px;
    }
    .deep-tag {
      padding: 7px 10px;
      border-radius: 999px;
      color: var(--info);
      background: var(--info-soft);
      font: 800 11px/1 Arial, sans-serif;
      text-transform: uppercase;
      letter-spacing: .06em;
      white-space: nowrap;
    }

    details {
      margin-top: 12px;
      border: 1px solid var(--line);
      border-radius: 14px;
      background: var(--paper);
      overflow: hidden;
    }
    summary {
      list-style: none;
      cursor: pointer;
      padding: 17px 19px;
      color: var(--ink-strong);
      font: 800 15px/1.35 Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 15px;
    }
    summary::-webkit-details-marker { display: none; }
    summary::after { content: "+"; color: var(--accent); font-size: 21px; }
    details[open] summary::after { content: "−"; }
    details[open] summary { background: var(--ochre-soft); }
    .details-body { padding: 19px; border-top: 1px solid var(--line); }
    .details-body p:last-child { margin-bottom: 0; }

    .formula {
      display: inline-block;
      margin: 7px 0 14px;
      padding: 10px 14px;
      border: 1px solid var(--line);
      border-radius: 10px;
      background: var(--paper-2);
      color: var(--ink-strong);
      font: 700 18px/1.2 "Courier New", monospace;
    }

    .source-list {
      counter-reset: source;
      margin: 21px 0 0;
      padding: 0;
      list-style: none;
      display: grid;
      gap: 10px;
    }
    .source-list li {
      counter-increment: source;
      display: grid;
      grid-template-columns: 34px minmax(0,1fr);
      gap: 11px;
      align-items: start;
      padding: 13px 14px;
      border: 1px solid var(--line);
      border-radius: 12px;
      background: var(--paper);
      font: 13px/1.45 Arial, sans-serif;
    }
    .source-list li::before {
      content: counter(source);
      width: 28px;
      height: 28px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      background: var(--accent-soft);
      color: var(--accent-deep);
      font-weight: 900;
    }
    .source-list b { color: var(--ink-strong); }
    .source-list span { color: var(--muted); }

    sup a { text-decoration: none; font: 800 10px/1 Arial, sans-serif; }

    .footer {
      padding: 24px clamp(27px, 5vw, 52px) 29px;
      border-top: 1px solid var(--line);
      color: var(--muted);
      background: var(--paper-2);
      font: 12px/1.5 Arial, sans-serif;
      display: flex;
      justify-content: space-between;
      gap: 20px;
      flex-wrap: wrap;
    }



    /* Persistent navigation marker: remains reachable throughout the document. */
    .floating-toc {
      position: fixed;
      right: max(18px, env(safe-area-inset-right));
      bottom: max(18px, env(safe-area-inset-bottom));
      z-index: 980;
      font-family: Arial, sans-serif;
    }

    .floating-toc-toggle {
      min-height: 52px;
      display: flex;
      align-items: center;
      gap: 9px;
      padding: 8px 11px 8px 9px;
      border: 1px solid color-mix(in srgb, var(--accent-deep) 76%, var(--line));
      border-radius: 999px;
      color: #f7f8ef;
      background: color-mix(in srgb, var(--accent-deep) 94%, var(--paper));
      box-shadow: 0 14px 34px rgba(35, 49, 34, .26), 0 2px 7px rgba(35, 49, 34, .14);
      cursor: pointer;
      transition: transform .18s ease, box-shadow .18s ease, background .18s ease;
    }

    .floating-toc-toggle:hover {
      transform: translateY(-2px);
      background: color-mix(in srgb, var(--accent) 92%, var(--accent-deep));
      box-shadow: 0 17px 38px rgba(35, 49, 34, .30), 0 3px 8px rgba(35, 49, 34, .16);
    }

    .floating-toc-toggle:focus-visible,
    .floating-toc-close:focus-visible {
      outline: 3px solid color-mix(in srgb, var(--ochre) 72%, white);
      outline-offset: 3px;
    }

    .floating-toc-icon {
      width: 35px;
      height: 35px;
      flex: 0 0 35px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      background: rgba(255, 255, 255, .13);
      font-size: 18px;
      line-height: 1;
    }

    .floating-toc-label {
      font: 800 12px/1 Arial, sans-serif;
      letter-spacing: .035em;
      white-space: nowrap;
    }

    .floating-toc-step {
      min-width: 27px;
      height: 27px;
      display: grid;
      place-items: center;
      border-radius: 999px;
      color: var(--accent-deep);
      background: #eef1df;
      font: 900 10px/1 Arial, sans-serif;
    }

    .floating-toc-chevron {
      opacity: .78;
      font-size: 12px;
      transition: transform .18s ease;
    }

    .floating-toc.is-open .floating-toc-chevron { transform: rotate(180deg); }

    .floating-toc-panel {
      position: absolute;
      right: 0;
      bottom: calc(100% + 11px);
      width: min(360px, calc(100vw - 24px));
      max-height: min(70vh, 610px);
      overflow: auto;
      overscroll-behavior: contain;
      border: 1px solid var(--line-strong);
      border-radius: 18px;
      background: color-mix(in srgb, var(--paper) 97%, transparent);
      box-shadow: 0 24px 58px rgba(35, 49, 34, .24);
      backdrop-filter: blur(14px);
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
      transform: translateY(9px) scale(.985);
      transform-origin: right bottom;
      transition: opacity .18s ease, transform .18s ease, visibility .18s ease;
    }

    .floating-toc.is-open .floating-toc-panel {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transform: translateY(0) scale(1);
    }

    .floating-toc-head {
      position: sticky;
      top: 0;
      z-index: 2;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 12px;
      align-items: center;
      padding: 15px 15px 13px;
      border-bottom: 1px solid var(--line);
      background: color-mix(in srgb, var(--paper) 96%, transparent);
      backdrop-filter: blur(12px);
    }

    .floating-toc-head small {
      display: block;
      margin-bottom: 4px;
      color: var(--muted);
      font: 800 9px/1.2 Arial, sans-serif;
      letter-spacing: .13em;
