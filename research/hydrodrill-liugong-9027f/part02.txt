     z-index: 2;
    }

    .meta span {
      border: 1px solid rgba(255,255,255,.18);
      background: rgba(255,255,255,.09);
      border-radius: 999px;
      padding: 7px 11px;
      font: 700 12px/1 Arial, sans-serif;
      color: #f2f4e9;
    }

    .document {
      background: var(--paper);
      border: 1px solid var(--line);
      border-radius: 30px;
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .section {
      padding: clamp(27px, 5vw, 52px);
      border-top: 1px solid var(--line);
      scroll-margin-top: 14px;
    }
    .section:first-child { border-top: 0; }
    .section:nth-child(even) { background: color-mix(in srgb, var(--paper-2) 48%, var(--paper)); }

    .section-kicker {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 12px;
      color: var(--accent);
      font: 800 12px/1.2 Arial, sans-serif;
      letter-spacing: .14em;
      text-transform: uppercase;
    }
    .section-kicker::before {
      content: attr(data-step);
      width: 31px;
      height: 31px;
      display: grid;
      place-items: center;
      border: 1px solid var(--line-strong);
      border-radius: 50%;
      color: var(--accent-deep);
      background: var(--paper);
      letter-spacing: 0;
      font-size: 11px;
    }

    h2, h3, h4 { color: var(--ink-strong); text-wrap: balance; }
    h2 {
      margin: 0 0 14px;
      font-size: clamp(28px, 4vw, 42px);
      line-height: 1.09;
      letter-spacing: -.025em;
    }
    h3 {
      margin: 30px 0 12px;
      font-size: clamp(21px, 2.4vw, 27px);
      line-height: 1.2;
    }
    h4 {
      margin: 24px 0 10px;
      font-size: 18px;
      line-height: 1.25;
    }
    p { margin: 0 0 16px; }
    .intro {
      max-width: 830px;
      color: var(--muted);
      font-size: 18px;
      line-height: 1.6;
    }

    .conclusion {
      margin-top: 24px;
      display: grid;
      grid-template-columns: 8px minmax(0, 1fr);
      border: 1px solid color-mix(in srgb, var(--accent) 35%, var(--line));
      border-radius: var(--radius-lg);
      overflow: hidden;
      background: var(--accent-soft);
      box-shadow: var(--shadow-soft);
    }
    .conclusion::before { content: ""; background: var(--accent); }
    .conclusion-body { padding: clamp(20px, 3.6vw, 32px); }
    .conclusion-label {
      margin: 0 0 8px;
      color: var(--accent-deep);
      font: 900 11px/1.2 Arial, sans-serif;
      letter-spacing: .16em;
      text-transform: uppercase;
    }
    .conclusion strong {
      display: block;
      color: var(--ink-strong);
      font-size: clamp(22px, 3.2vw, 31px);
      line-height: 1.25;
    }
    .conclusion p { margin: 12px 0 0; color: var(--ink); }

    .critical-strip {
      margin-top: 18px;
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 12px;
      align-items: center;
      padding: 15px 17px;
      border: 1px solid color-mix(in srgb, var(--danger) 34%, var(--line));
      border-radius: var(--radius-md);
      background: var(--danger-soft);
    }
    .critical-strip .symbol {
      width: 34px;
      height: 34px;
      display: grid;
      place-items: center;
      border-radius: 50%;
      background: var(--danger);
      color: white;
      font: 900 17px/1 Arial, sans-serif;
    }
    .critical-strip b { color: var(--ink-strong); }
    .critical-strip span { color: var(--muted); }

    .quick-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 13px;
      margin-top: 18px;
    }
    .quick-card {
      min-width: 0;
      padding: 18px;
      border: 1px solid var(--line);
      border-radius: var(--radius-md);
      background: var(--paper);
    }
    .quick-card small {
      display: block;
      margin-bottom: 7px;
      color: var(--muted);
      font: 800 10px/1.2 Arial, sans-serif;
      letter-spacing: .12em;
      text-transform: uppercase;
    }
    .quick-card b {
      display: block;
      color: var(--ink-strong);
      font-size: 21px;
      line-height: 1.18;
    }
    .quick-card p { margin: 8px 0 0; color: var(--muted); font-size: 14px; }

    .reading-levels {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      margin-top: 25px;
      border: 1px solid var(--line);
      border-radius: var(--radius-md);
      overflow: hidden;
      background: var(--line);
    }
    .reading-level {
      padding: 15px 17px;
      background: var(--paper);
      font-family: Arial, sans-serif;
    }
    .reading-level b { display: block; font-size: 13px; color: var(--ink-strong); }
    .reading-level span { display: block; margin-top: 3px; font-size: 12px; color: var(--muted); }

    .flow-wrap {
      margin-top: 24px;
      padding: clamp(18px, 3vw, 26px);
      border: 1px solid var(--line);
      border-radius: var(--radius-lg);
      background: linear-gradient(180deg, color-mix(in srgb, var(--paper) 70%, transparent), var(--paper-2));
      overflow-x: auto;
    }
    .flow {
      min-width: 850px;
      display: grid;
      grid-template-columns: repeat(7, minmax(110px, 1fr));
      gap: 19px;
      align-items: stretch;
    }
    .flow-node {
      position: relative;
      min-height: 132px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 15px;
      border: 1px solid var(--line);
      border-radius: 14px;
      background: var(--paper);
      box-shadow: 0 3px 10px rgba(43,58,40,.05);
    }
    .flow-node:not(:last-child)::after {
      content: "→";
      position: absolute;
      right: -17px;
      top: 50%;
      transform: translateY(-50%);
      color: var(--ochre);
      font: 900 23px/1 Arial, sans-serif;
    }
    .flow-no {
      width: 30px;
      height: 30px;
      display: grid;
      place-items: center;
      border-radius: 9px;
      background: var(--ochre-soft);
      color: var(--ochre);
      font: 900 11px/1 Arial, sans-serif;
    }
    .flow-node b {
      display: block;
      margin-top: 12px;
      color: var(--ink-strong);
      font: 800 14px/1.25 Arial, sans-serif;
    }
    .flow-node span {
      display: block;
      margin-top: 5px;
      color: var(--muted);
      font: 12px/1.35 Arial, sans-serif;
    }
    .flow-node.focus {
      border-color: var(--accent);
      background: var(--accent-soft);
      transform: translateY(-4px);
    }
    .flow-node.focus .flow-no { background: var(--accent); color: white; }

    .diagram-caption {
      margin: 12px 0 0;
      color: var(--muted);
      font: 12px/1.45 Arial, sans-serif;
    }

    .decision-map {
      position: relative;
      margin-top: 24px;
      padding: 24px;
      border: 1px solid var(--line);
      border-radius: var(--radius-lg);
      background: var(--paper);
      overflow: hidden;
    }
    .decision-root {
      width: min(540px, 100%);
      margin: 0 auto 28px;
      padding: 16px 20px;
      border-radius: 999px;
      background: var(--info-soft);
      border: 1px solid color-mix(in srgb, var(--info) 35%, var(--line));
      color: var(--ink-strong);
      text-align: center;
      font: 800 15px/1.35 Arial, sans-serif;
    }
    .decision-root::after {
      content: "↓";
      display: block;
      height: 0;
      position: relative;
      top: 19px;
      color: var(--ochre);
      font-size: 22px;
    }
    .decision-branches {
      display: grid;
      grid-template-columns: repeat(4, minmax(0,1fr));
      gap: 12px;
    }
    .branch {
      padding: 17px;
      border: 1px solid var(--line);
      border-radius: 14px;
      background: var(--